# SettleSense Environment Configuration Example
# -----------------------------------------
# Copy this file to .env and modify the values as needed

# Application Configuration
# -------------------------
# Debug mode (True/False)
DEBUG=True

# Secret key used for session encryption (change to a random string in production!)
SECRET_KEY=changeme_this_is_not_secure_in_production

# Server Configuration
# -------------------
# Port number for the server to listen on
PORT=5000

# Host interface to bind to (use 0.0.0.0 to listen on all interfaces)
HOST=127.0.0.1

# Database Configuration
# ---------------------
# The database path is automatically set to instance/debts.sqlite
# No need to change unless you want to use a different location.
# DATABASE_PATH=/custom/path/to/database.sqlite
